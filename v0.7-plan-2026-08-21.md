# dsh-mneme v0.7 版本策划书 + 分工安排（2026-08-21）

> 状态：**待桉桉过目** → 通过后**晚上错峰执行**（兼顾火山 coding plan ~1200 次/5 小时额度，分阶段推进，不在同一时段烧光）。
> 当前版本：v0.6.6（710 全绿）。本策划基于 README 路线图 + Discussion #21 + 社区 heptaspirit 调研（`docs/AGENT_MEMORY_RESEARCH.md`，已归档为 v0.7 参考输入）。

---

## 一、版本定位（调整：v0.7 不着急，先做 v0.6.7）

**近期先做：v0.6.7 —— Issue #20 面板删除按钮**（兑现对社区 lqs50 的承诺，快速小版本）。

**v0.7 主题：自进化记忆（heat 热度 + 兴趣漂移）—— 桉桉 2026-08-21 拍板：不着急，列为远期**，先把 v0.6.x 的欠账收干净再说。

一句话：v0.7 让记忆库不只是"存得准、召得回"，还能**根据访问热度自我衰减、识别兴趣漂移、降级冷记忆**，进化为会"进化"的记忆体。核心来自 #21（简化幂律 + type 差异化半衰期），社区调研补足数据前提与图谱增强。

**复用承接（不引入新依赖/新概念）：** 全部落在现有 `recall_runs` 表 / `TYPE_DECAY` / sleep 批处理 / `entity_attrs` valid_from-until / api.js 的 ego-BFS 上。图数据库（Neo4j/Graphiti）明确不做——SQLite 递归 CTE 实测够用。

---

## 二、范围拆分

### v0.6.7 —— Issue #20 记忆面板删除按钮（近期，优先做）
| # | 功能 | 说明 | 承接机制 |
|---|------|------|----------|
| ⭐1 | **面板删除按钮** | 记忆库界面**直接删除自己不需要的记忆**（社区 lqs50 提，issue #20 仍 OPEN）。后端 storage/`memory_delete` 工具已有 → **补一个面板 HTTP DELETE 端点 + 前端「选中删除」按钮**（永久删除带二次确认；可挂到目录 view / 详情页）| store.deleteMemory + api.js + 面板 |
- **验收：** 全绿；面板可选中删除记忆（含目录视图入口）；**收掉 issue #20** 并回帖致谢 lqs50。
- **依赖关系：** 独立小版本，不需要等 v0.7。夜间一个窗口即可完成。

### v0.7.0 —— heat 热度 + 兴趣漂移核心（远期，⏸ 不着急）
| # | 功能 | 说明 | 承接机制 |
|---|------|------|----------|
| 1 | **heat 模型** | 简化幂律衰减 + `TYPE_DECAY`（type 差异化半衰期）；**公式预留信号接口**（新列/metadata，以后加多信号是增量不重构）| #21 已定方向 |
| 2 | **recall_runs 数据前提** | 记录**默认开、消费默认关**；candidates 打 **`injected` 标记**（区分"被召回"vs"被注入上下文"两个强度）；**90 天滚动清理**防膨胀 | recall_runs / touchRecalled |
| 3 | **sleep 降级联合判定** | 现状 30/90 天硬阈值不看 importance/type，高价值 preference 也会被降级 → 改为 `heat<阈值 && importance<5 && type 非免疫`，双保护，仍以时间为主 | sleep 批处理 / archived |
| 4 | **updated_at 不算访问** | 合并/更新刷 updated_at 不应计为访问，防 autoDream 合并动作干扰新鲜度 | 时间戳语义修正 |
| 5 | **heat 投影实体层** | 实体热 = 关联记忆 heat 聚合；ego-graph 前端节点大小/明暗随热变化，兴趣漂移**在图上可见**（成本≈0，heat 本就 v0.7 改）| entities 表 + 前端 |
- **验收：** 全绿（目标 710 → 740+）；heat 全部 opt-in（`heatEnabled` 默认保守开启、不破坏现有）。
- **状态：** ⏸ 桉桉拍板不着急，先做完 v0.6.x 收尾再启动。

### v0.7.1 —— 图谱数据积累 + 检索/关系增强（次轮）
| # | 功能 | 说明 | 承接机制 |
|---|------|------|----------|
| 6 | **实体提取移入 sleep 批量** | 写入路径实时抽实体成本高 → 挪进 sleep 批量，**写入路径零 LLM 成本**；代价是图谱滞后一个 sleep 周期（长期关系网无感）| sleep LLM 阶段模式 |
| 7 | **sleep 批量实体消歧** | extractor 目前不合并同义词，全称/简称成孤立节点 → 仿 autoDream 的 winner/loser + 审计，或规则兜底（小写/去空格）| findEntityByName |
| 8 | **1-hop 邻域回流检索** | `entity:X` 顺带召回相邻实体记忆——把图谱从"展示"变成"召回信号"（差异化价值）| api.js ego-BFS |
| 9 | **关系时效** | `entity_relations` 补 `valid_until` + `superseded` 标记（不删只标，保留历史回溯），换方案后旧关系不一直挂着 | entity_attrs 的 valid_from/until 模式 |
- **验收：** 全绿；图谱数据能攒起来，前端可见热投影。

### 远期 / 等 DSH（不在本次做）
- 跨 workspace 记忆共享（README v0.7.0+ 标注，**等 DSH 支持**）；多模态记忆（本地化定位冲突，排除）；参数化记忆 MemoryLLM（模型层路线，基本放弃）；图数据库/PPR/社区子图（CTE 够用，暂缓）。

---

## 三、分工安排（多模型协作，服从派发架构）

| 角色 | 模型 | 负责 | 量级 |
|------|------|------|------|
| **协调 + 方案 + 把关** | qwen3.7-max | 拆任务、定义 heat 公式/TYPE_DECAY 表格/schema、每步评审整合 | 中 |
| **核心代码 + 复杂测试** | kimi-k3 | heat 模型、sleep 降级判定、1-hop 回流、实体消歧、90 天清理等实现 + 全绿测试 | 高 |
| **轻量校验/快检** | glm-4.5-air | 小步 diff 校验、配置项联动、回归抽查 | 低 |
| **文档同步** | qwen3.6-plus | CHANGELOG + README 双表（root+子目录、中英），ROADMAP 状态对 | 低 |
| **集成 + 发布** | 主模型（Ivresse） | 汇总、核验、**报桉桉批准后**发布；一次只发一个 sub-version | — |

> 派发铁律：发布/改动前先征得桉桉许可；复杂编程/测试派 kimi-k3；REANME/CHANGELOG 根+子目录中英都要同步，避免漏改。

## 四、执行节奏（错峰，尊重 ~1200 次/5 小时）
- **窗口**：每晚 7 点后 / 周末，一次只推一个 sub-step（先 0.7.0，休息/量量再用 0.7.1）。
- **每步收尾**：跑全绿 → 更新 CHANGELOG/README → 记 activeContext/progress → 停。
- **不集中突发**：同一窗口不做超长连续生成，做完即停，额度留着。

---

## 五、待桉桉拍板（已定项已在文内标注）
✅ 已定：**Issue #20 → v0.6.7**（快速兑现）；**v0.7.0 不着急**（远期，先收 v0.6.x 账）。
> 剩下细节到真正开工前再定即可：heat 默认开/关、v0.7.0 与 v0.7.1 节奏。