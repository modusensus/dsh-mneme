# 覆盖率口径：lib/ 是 src/ 的机器同步副本（scripts/check-sync.js 强制逐文件一致，
# CI 前置校验），没有独立测试价值——测试全部走 src/。把 lib/ 计入等于给 src/ 的
# 每一行双倍权重，真实覆盖被腰斩（本地实测：全量 66.7% vs 排除 lib 后 93.9% 行覆盖）。
# 忽略后徽章反映唯一事实源 src/ 的真实覆盖。
ignore:
  - "dsh-mneme/lib/**"
