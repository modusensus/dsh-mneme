# onnxruntime-node's postinstall tries to fetch CUDA binaries from the NuGet
# feed, which redirects (302) and fails on most installs. The CPU core binary
# ships bundled with the npm package, so skipping the CUDA fetch is safe and
# keeps `npm install` green.
onnxruntime_node_install=skip
